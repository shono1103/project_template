# MEMORY

> `/reload-project` で自動生成される。手で編集しない。
> 最終更新: 2026-08-01 / 概算 213 トークン (上限 1,500)

## プロジェクト概要

プロジェクト管理リポジトリのセットアップ段階。
日報 (`daily/`)、案件とタスク (`job/`)、Q&A (`qa/`)、関連リポジトリ (`repos/`) の
テンプレートと運用スクリプトを整備した状態で、実データはまだ無い。

## 進行中の job

なし (`job/` はテンプレートのみ)

## 直近の日報

なし (`./daily/create_daily.sh` で当日分を作成する)

## 未解決の QA

なし

## submodule

なし (`./repos/add_submodule.sh` で追加する)
