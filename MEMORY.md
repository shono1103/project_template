# MEMORY

> `/reload-project` で自動生成される。手で編集しない。
> 最終更新: 2026-08-10 / 概算 289 トークン (上限 1,500)

## プロジェクト概要

プロジェクト管理リポジトリのセットアップ段階。
日報 (`daily/`)、案件とタスク (`job/`)、Q&A (`qa/`)、ドキュメント (`docs/`)、
関連リポジトリ (`repos/`) のテンプレートと運用スクリプトを整備した状態で、実データはまだ無い。
日報は日ごとに `mine/` (自分) と `agents/<agent名>/` (agent ごと) に分かれる ([daily/README.md](daily/README.md))。

## 進行中の job

なし (`job/` はテンプレートのみ)

## 直近の日報

なし (`./daily/create_daily.sh` で当日分を作成する)

## 未解決の QA

なし

## docs

なし (`docs/{official,unofficial,personal}/` は空、[docs/README.md](docs/README.md) のみ)

## submodule

なし (`./repos/add_submodule.sh` で追加する)
